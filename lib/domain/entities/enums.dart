enum InvoiceStatus { draft, issued, partiallyPaid, paid, cancelled }
enum PaymentMethod { cash, bank }
enum TransactionDirection { incoming, outgoing }
enum UserRole { admin, accountant, viewer }
enum ExpenseType { operating, payroll, utility, other }
